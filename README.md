
  # Create case study

  This is a code bundle for Create case study. The original project is available at https://www.figma.com/design/xrhh3CkVsu6krc5ZggXduV/Create-case-study.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  